// Optional: For caching chatbot responses if needed in the future
abstract class ChatbotLocalDataSource {
  // Can be implemented later for caching
}

class ChatbotLocalDataSourceImpl implements ChatbotLocalDataSource {
  const ChatbotLocalDataSourceImpl();
}